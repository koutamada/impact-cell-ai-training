# 中級編 Part 6：AI文書要約ツール 受入チェックリスト

- 対象仕様書：`docs/intermediate/part06-document-summarizer/part06-document-summarizer-spec.md`
- 実装先（将来）：`apps/intermediate/part06-document-summarizer/`
- Edge Function（将来）：`supabase/functions/summarize-document/`
- 判定原則：初期状態は全項目未チェックとし、実際に確認した項目だけを`[x]`へ変更する
- 区分：`[必須A]`＝明示機能、`[必須B]`＝安全・検証の補完要件、`[任意]`＝MVP合格条件外
- 証跡：ブラウザ、375px、Network、Supabase管理画面、SQL、Edge Functionログ、stub／障害注入、自動試験等を項目ごとに記録する

---

## A. 成果物・非回帰・設定

- [x] **A-01：Part 6成果物構成** `[必須B]`
  - 操作・条件：リポジトリとGit差分でPart 6のHTML、CSS、JS、config、vendor、migration、Edge Function、仕様書、受入表を確認する。
  - 期待結果：仕様どおりに静的フロントとSupabase配備物が分離され、必要ファイルが揃っている。
  - 確認方法：Git、ファイル一覧。

- [x] **A-02：Part 1〜5非変更** `[必須B]`
  - 操作・条件：実装前後のGit差分とmigration対象を比較する。
  - 期待結果：既存Part 1〜5のファイル、テーブル、Policy、RPC、Functionを意図せず変更していない。
  - 確認方法：Git diff、SQLレビュー。

- [x] **A-03：未追跡ファイル保護** `[必須B]`
  - 操作・条件：既存の未追跡ファイルを含む`git status`を実装前後で確認する。
  - 期待結果：Part 6以外の未追跡ファイルを変更、削除、commitしていない。
  - 確認方法：Git status、ハッシュ比較。

- [ ] **A-04：設定未完了表示** `[必須A]`
  - 操作・条件：公開configをプレースホルダーまたは欠落状態で開く。
  - 期待結果：外部要求や未捕捉例外を起こさず、Supabase URLとpublishable keyの設定方法を案内する。
  - 確認方法：ブラウザ、Console、Network。

- [x] **A-05：公開configの秘密非混入** `[必須B]`
  - 操作・条件：config、HTML、JS、Git履歴を検索する。
  - 期待結果：公開可能なSupabase URLとpublishable key以外のAPI key、service role key、JWT、DB秘密を含まない。
  - 確認方法：静的検索、Git diff。

- [ ] **A-06：Part 5再利用処理の再検証** `[必須B]`
  - 操作・条件：Part 5由来の認証、CORS、通知、後着応答処理についてPart 6の試験結果を確認する。
  - 期待結果：Part 5を完全検証済みと仮定せず、Part 6で必要な試験が独立して記録されている。
  - 確認方法：受入記録、コードレビュー。

- [ ] **A-07：GitHub Pages静的公開** `[必須A]`
  - 操作・条件：公開URLを開き、配信設定とNetworkを確認する。
  - 期待結果：ビルド不要の静的フロントが表示され、OpenAI keyを含むサーバー資材は配信されない。
  - 確認方法：GitHub設定、ブラウザ、Network。

- [ ] **A-08：Supabase設定整合** `[必須B]`
  - 操作・条件：ローカルとリモートのAuth redirect、Data API、Function JWT設定を確認する。
  - 期待結果：Part 6のlocalhost／GitHub Pages URLが許可され、新テーブル自動公開に依存せず、JWT検証が有効である。
  - 確認方法：`config.toml`、Supabase管理画面、CLI。

## B. 認証とセッション

- [ ] **B-01：アカウント作成** `[必須B]`
  - 操作・条件：有効／無効なメール・パスワードで登録する。
  - 期待結果：Part 5方針に沿う検証と安全な固定エラーになり、確認が必要な場合は待機状態を示す。
  - 確認方法：ブラウザ、Network、Supabase Auth。

- [x] **B-02：ログイン成功** `[必須B]`
  - 操作・条件：有効な利用者でログインする。
  - 期待結果：本人の入力画面と要約履歴だけが表示される。
  - 確認方法：ブラウザ、Network、SQL。

- [ ] **B-03：ログイン失敗** `[必須B]`
  - 操作・条件：不正な資格情報でログインする。
  - 期待結果：アカウントの存在やSupabase生エラーを表示しない。
  - 確認方法：ブラウザ、Console、Network。

- [ ] **B-04：セッション復元** `[必須B]`
  - 操作・条件：ログイン済みで再読込する。
  - 期待結果：認証確認後に本人の画面と履歴を復元し、確認前に保護情報を一瞬表示しない。
  - 確認方法：ブラウザ、低速化試験。

- [ ] **B-05：ログアウト時消去** `[必須B]`
  - 操作・条件：原文、選択ファイル、生成結果を表示した状態でログアウトする。
  - 期待結果：原文、File参照、Object URL、要約、根拠、履歴、request IDをメモリとDOMから消す。
  - 確認方法：ブラウザ、DevTools、メモリ／DOM確認。

- [ ] **B-06：セッション失効** `[必須B]`
  - 操作・条件：無効または期限切れtokenでDBとFunctionを呼ぶ。
  - 期待結果：401相当で保護内容を消し、再ログインを案内し、後着応答を採用しない。
  - 確認方法：Network、stub、ブラウザ。

- [ ] **B-07：パスワード再設定** `[必須B]`
  - 操作・条件：localhostとGitHub Pagesから再設定要求・完了を行う。
  - 期待結果：正規化されたPart 6 URLへ戻り、新パスワード設定後に通常利用できる。
  - 確認方法：ブラウザ、Supabase Auth、メール試験。

- [ ] **B-08：利用者切替** `[必須B]`
  - 操作・条件：利用者Aで原文を入力後ログアウトし、利用者Bでログインする。
  - 期待結果：Aの原文、ファイル名、結果、履歴がBへ表示されない。
  - 確認方法：ブラウザ、DOM、Network。

- [x] **B-09：登録結果のアカウント列挙防止** `[必須B]`
  - 操作・条件：公開環境の登録フォームへ既存アカウントを入力する。
  - 期待結果：新規利用者と既存利用者を区別せず、「登録手続きを受け付けました」と安全な共通説明を表示し、登録済みであることを明示しない。
  - 確認方法：公開環境の実ブラウザ、画面文言、認証フロー。

- [x] **B-10：新規登録メールと確認リンク** `[必須B]`
  - 操作・条件：完全な新規アカウントを登録し、確認メールを実受信してリンクを開く。
  - 期待結果：確認メールが届き、リンクから許可済みのPart 6公開URLへ復帰してログインできる。
  - 確認方法：公開環境、受信メール、Supabase Auth、redirect URL。

- [x] **B-11：公開環境のパスワード再設定** `[必須B]`
  - 操作・条件：GitHub Pages公開環境からパスワード再設定を要求し、受信メールのリンクを開いて新しいパスワードを設定する。
  - 期待結果：再設定メールが届き、リンクからPart 6へ復帰してパスワードを変更できる。
  - 確認方法：公開環境、受信メール、Supabase Auth、実ブラウザ。

## C. 入力方式と共通検証

- [ ] **C-01：直接入力への切替** `[必須A]`
  - 操作・条件：ファイル選択後に直接入力へ切り替える。
  - 期待結果：入力方式が明確に変わり、ファイルと本文を同時送信しない。
  - 確認方法：ブラウザ、Network payload。

- [ ] **C-02：ファイル入力への切替** `[必須A]`
  - 操作・条件：直接入力後にファイル入力へ切り替える。
  - 期待結果：誤って直接入力をファイルと同時送信せず、取消可能である。
  - 確認方法：ブラウザ、Network payload。

- [ ] **C-03：空入力** `[必須A]`
  - 操作・条件：直接入力空欄、空白だけ、ファイル未選択で送信する。
  - 期待結果：入力に関連付いた固定エラーを表示し、DB、Function、OpenAIを呼ばない。
  - 確認方法：ブラウザ、Network、SQL。

- [ ] **C-04：入力競合** `[必須B]`
  - 操作・条件：DOMまたは直接要求で本文とファイルを同時指定する。
  - 期待結果：クライアントとEdge Functionの双方で拒否され、OpenAIを呼ばない。
  - 確認方法：改変要求、Functionログ、Network。

- [ ] **C-05：複数ファイル** `[必須B]`
  - 操作・条件：直接要求またはドロップで複数ファイルを与える。
  - 期待結果：1ファイル上限として拒否し、一部だけを黙って採用しない。
  - 確認方法：ブラウザ、改変要求。

- [ ] **C-06：ファイル名安全表示** `[必須B]`
  - 操作・条件：HTML、引用符、制御文字、長い名前、パス区切りを含むファイル名を表示する。
  - 期待結果：コードを実行せず、安全に折り返し、レイアウトを破壊しない。
  - 確認方法：ブラウザ、Console、XSS試験。

- [ ] **C-07：拡張子偽装** `[必須B]`
  - 操作・条件：拡張子とMIME／シグネチャが一致しないファイルを送る。
  - 期待結果：サーバー側でも`unsupported_file_type`として拒否し、OpenAIを呼ばない。
  - 確認方法：改変ファイル、Network、Functionログ。

- [ ] **C-08：サイズ境界** `[必須A][必須B]`
  - 操作・条件：直接入力100,000コードポイント、TXT／Markdown 1MiBかつ抽出後100,000コードポイント、PDF／Office 6MiBの各上限直前、一致、1単位超過を送る。
  - 期待結果：上限以下だけを受理し、超過時は切り捨てず`input_too_large`とする。
  - 確認方法：生成データ、Network、Functionログ。

- [ ] **C-09：巨大入力** `[必須A][必須B]`
  - 操作・条件：要求body、100,000コードポイント、6MiB、1,000 segmentの上限を大幅に超える入力を送る。
  - 期待結果：資源枯渇前に拒否し、部分送信・AI呼出しを行わない。
  - 確認方法：負荷試験、Network、ログ、SQL。

- [ ] **C-10：ファイル読込失敗** `[必須A]`
  - 操作・条件：ブラウザのFile読込をstubで失敗させる。
  - 期待結果：`file_read_failed`を表示し、選択設定を維持し、外部要求しない。
  - 確認方法：ブラウザstub、自動試験。

- [ ] **C-11：Word／PowerPoint注意表示** `[必須A]`
  - 操作・条件：ファイル入力画面をPCと375pxで確認する。
  - 期待結果：画像、図表、レイアウトが十分反映されない可能性と、必要ならPDF変換する案内が読める。
  - 確認方法：ブラウザ、375px、アクセシビリティツリー。

- [ ] **C-12：アプリ独自OCRの対象外表示** `[必須A]`
  - 操作・条件：入力説明とPDF処理説明を確認する。
  - 期待結果：アプリ独自のOCR、OCR精度設定、OCR結果編集は対象外だが、PDFはResponses APIの標準処理へ委ねることを案内する。
  - 確認方法：ブラウザ、375px。

## D. 直接入力・TXT・Markdown

- [ ] **D-01：直接入力正常系** `[必須A]`
  - 操作・条件：複数段落の日本語本文を直接入力して要約する。
  - 期待結果：原文順のセグメントIDが付与され、設定に沿う要約と実在根拠IDが保存・表示される。
  - 確認方法：ブラウザ、Network、SQL。

- [ ] **D-02：直接入力境界** `[必須A]`
  - 操作・条件：最小1段落、100,000コードポイント一致、100,001コードポイントを送る。
  - 期待結果：許可範囲だけ成功し、超過文は切り捨てず拒否する。
  - 確認方法：ブラウザ、Network payload。

- [ ] **D-03：段落とセグメントID** `[必須A][必須B]`
  - 操作・条件：CRLF、CR、前後の空白行、連続空行、Markdown見出し・リスト、1,000コードポイント超の文を含む文書を入力する。
  - 期待結果：LF正規化、空行境界、見出し独立、原則1リスト1segment、文境界優先分割、最後の手段の安全な1,000コードポイント分割を行い、空segmentなしで`S0001`から原文順に採番する。
  - 確認方法：単体試験、Network。

- [ ] **D-04：TXT正常系** `[必須A]`
  - 操作・条件：UTF-8およびUTF-8 BOMの複数段落TXTを個別に要約する。
  - 期待結果：文字化けせずテキスト化され、セグメントID、要約、根拠対応が生成される。
  - 確認方法：ブラウザ、Network、SQL。

- [ ] **D-05：空TXT** `[必須A]`
  - 操作・条件：0 byteおよび空白だけのTXTを選択する。
  - 期待結果：`document_unreadable`または入力不足として送信前に拒否し、OpenAIを呼ばない。
  - 確認方法：ブラウザ、Network。

- [ ] **D-06：TXT文字コード不正** `[必須B]`
  - 操作・条件：Shift_JIS、UTF-16、不正UTF-8のTXTとMarkdownを選ぶ。
  - 期待結果：文字化けしたまま送信せず、`file_read_failed`としてUTF-8への変換を案内する。
  - 確認方法：テストファイル、ブラウザ。

- [x] **D-07：Markdown正常系** `[必須A]`
  - 操作・条件：見出し、箇条書き、コード記法、リンクを含むMarkdownを要約する。
  - 期待結果：確定した段落規則でセグメント化され、内容を要約できる。
  - 確認方法：ブラウザ、Network、SQL。

- [ ] **D-08：Markdown非実行** `[必須B]`
  - 操作・条件：HTML、script、イベント属性、危険URLを含むMarkdownを読み込む。
  - 期待結果：HTMLとして実行せずプレーンテキストとして安全に表示・処理する。
  - 確認方法：XSS試験、CSP、Console。

- [ ] **D-09：空Markdown** `[必須A]`
  - 操作・条件：0 byte、空白だけ、構文記号だけで実質本文のないMarkdownを選ぶ。
  - 期待結果：確定した空文書規則に従い、抽出不能ならOpenAI前に拒否する。
  - 確認方法：ブラウザ、Network。

- [ ] **D-10：正規化と文書ハッシュ** `[必須B]`
  - 操作・条件：直接入力系の改行正規化後テキスト、Office系raw bytes、ファイル名・設定だけを変えた入力でhashを比較する。
  - 期待結果：SHA-256を使い、直接入力系はIDを除く要約対象UTF-8 bytes、PDF／Officeはraw bytesを対象とし、ファイル名・要約設定を含めない。
  - 確認方法：単体試験、SQL。

- [ ] **D-11：表示原文とsegment対応** `[必須A][必須B]`
  - 操作・条件：改行形式、長い段落、Markdown見出し・リストを含む原文から根拠移動する。
  - 期待結果：表示用原文は正規化前の見た目を可能な限り維持しつつ、AI送信用segmentへ正しく対応する。
  - 確認方法：ブラウザ、単体試験。

## E. PDF・Word・PowerPoint

- [x] **E-01：テキストPDF正常系** `[必須A]`
  - 操作・条件：複数ページのテキストPDFを要約する。
  - 期待結果：Edge Functionから`input_file`として1回だけOpenAIへ送られ、要約と短い根拠候補が保存される。
  - 確認方法：Network、OpenAI stub、SQL。

- [ ] **E-02：PDF位置情報** `[必須A][必須B]`
  - 操作・条件：位置を特定可能／不可能なPDF応答を模擬する。
  - 期待結果：特定できる場合だけ検証済みschemaのページ等を表示し、不明な位置を捏造・必須化しない。
  - 確認方法：stub、ブラウザ。

- [ ] **E-03：画像PDF** `[必須A]`
  - 操作・条件：文字層のない画像中心PDFを送る。
  - 期待結果：送信前に画像PDFとして拒否せず、Responses APIの標準PDF処理へ委ねる。抽出テキストやページ画像から結果を得られれば採用し、OpenAIでも読み取れない場合だけ`document_unreadable`とする。
  - 確認方法：テストPDF、OpenAI stub、Network、SQL。

- [ ] **E-04：破損PDF** `[必須A]`
  - 操作・条件：headerだけ、途中切断、構造不正のPDFを送る。
  - 期待結果：基本検証で判別できる偽装等は事前拒否する。独自解析なしでは分からない破損はOpenAIへ送信してよく、読取不能なら生エラーを隠して`document_unreadable`とする。
  - 確認方法：テストPDF、Functionログ、SQL。

- [ ] **E-05：暗号化PDF** `[必須B]`
  - 操作・条件：パスワード保護PDFを送る。
  - 期待結果：完全な事前検出を要求せず、パスワードを要求・保存しない。OpenAIが読めなければ`document_unreadable`とする。
  - 確認方法：テストPDF、ブラウザ。

- [x] **E-06：DOCX正常系** `[必須A]`
  - 操作・条件：見出し、本文、表、画像を含むDOCXを要約する。
  - 期待結果：Edge Functionから`input_file`として1回だけ送られ、主にテキストに基づく要約と根拠候補を返す。
  - 確認方法：Network、OpenAI stub、SQL。

- [ ] **E-07：DOCX制約表示** `[必須A]`
  - 操作・条件：DOCX選択時と結果画面を確認する。
  - 期待結果：画像、図表、レイアウトの反映が不完全になり得る注意とPDF変換案内を表示する。
  - 確認方法：ブラウザ、375px。

- [ ] **E-08：破損DOCX** `[必須A]`
  - 操作・条件：空、ZIP構造不正、必須部品欠落、展開上限超過のDOCXを送る。
  - 期待結果：アプリ側で展開・独自解析せず、基本検証後に`input_file`として送る。OpenAIが読めなければ生エラーを隠して`document_unreadable`とする。
  - 確認方法：テストファイル、Functionログ、SQL。

- [ ] **E-09：DOC正常系** `[必須A]`
  - 操作・条件：旧形式DOCを要約する。
  - 期待結果：Edge Functionから`input_file`へ直接送信し、テキスト中心の要約、根拠候補、注意表示を示す。アプリ側で変換・抽出しない。
  - 確認方法：ブラウザ、Network、stub、SQL。

- [ ] **E-10：破損DOC** `[必須A]`
  - 操作・条件：破損または偽装DOCを送る。
  - 期待結果：基本検証で判別できなければ`input_file`へ送信してよく、OpenAIが読めない場合に安全な`document_unreadable`へ分類する。
  - 確認方法：テストファイル、Functionログ。

- [x] **E-11：PPTX正常系** `[必須A]`
  - 操作・条件：複数スライド、ノート、図表を含むPPTXを要約する。
  - 期待結果：Edge Functionから`input_file`へ直接送信し、テキスト中心の要約、短い根拠候補、任意のスライド位置を示す。アプリ側で展開・抽出しない。
  - 確認方法：ブラウザ、Network、stub、SQL。

- [ ] **E-12：PPT正常系** `[必須A]`
  - 操作・条件：旧形式PPTを要約する。
  - 期待結果：Edge Functionから`input_file`へ直接送信し、図表等の制約と根拠候補注意を示す。アプリ側で変換・抽出しない。
  - 確認方法：ブラウザ、Network、stub、SQL。

- [ ] **E-13：破損PPT／PPTX** `[必須A]`
  - 操作・条件：破損、偽装、展開上限超過のPPT／PPTXを送る。
  - 期待結果：基本検証で判別できなければ`input_file`へ送信してよく、OpenAIが読めない場合に生エラーを隠して`document_unreadable`とする。
  - 確認方法：テストファイル、Functionログ、SQL。

- [ ] **E-14：Office埋め込み要素非実行** `[必須B]`
  - 操作・条件：マクロ、外部リンク、埋め込みオブジェクトを含むOffice文書を送る。
  - 期待結果：実行・外部取得せず、許可された文書データだけを処理する。
  - 確認方法：Network、解析stub、セキュリティレビュー。

- [x] **E-15：Office解析ライブラリ不使用** `[必須A]`
  - 操作・条件：依存関係、vendor、フロント、Edge Functionを確認する。
  - 期待結果：DOC、DOCX、PPT、PPTXを展開、変換、本文抽出する処理やOffice解析ライブラリがない。
  - 確認方法：静的検索、依存関係レビュー。

- [ ] **E-16：全ファイル形式のraw hash** `[必須B]`
  - 操作・条件：PDF、DOC、DOCX、PPT、PPTXで同一bytes／異なるbytes／ファイル名だけ変更したファイルを送る。
  - 期待結果：raw bytesのSHA-256が使われ、ファイル名と要約設定でhashが変わらない。
  - 確認方法：単体試験、Network、SQL。

- [x] **E-17：根拠候補注意表示** `[必須A]`
  - 操作・条件：PDF、DOC、DOCX、PPT、PPTXの結果と履歴再表示を確認する。
  - 期待結果：「AIが選定した根拠候補で、厳密な証明ではない」旨を毎回確認できる。
  - 確認方法：ブラウザ、375px、アクセシビリティツリー。

## F. 要約設定とAI指示

- [ ] **F-01：初期値** `[必須A]`
  - 操作・条件：初回表示、生成成功後、入力取消後を確認する。
  - 期待結果：初期値が「重要点のみ」「標準」である。
  - 確認方法：ブラウザ。

- [ ] **F-02：重要点のみ** `[必須A]`
  - 操作・条件：主要論点と補助例を含む文書を`key_points`で要約する。
  - 期待結果：重要論点を選び、反復や細かな例示を不必要に独立項目化しない。
  - 確認方法：実AI、固定評価文書、レビュー。

- [ ] **F-03：全体を網羅** `[必須A]`
  - 操作・条件：複数章の文書を`comprehensive`で要約する。
  - 期待結果：主要章・論点をできるだけ漏らさず、薄い章を項目数確保のため水増ししない。
  - 確認方法：実AI、固定評価文書、レビュー。

- [ ] **F-04：短く** `[必須A]`
  - 操作・条件：重要な数値・固有名詞を含む文書を`brief`で要約する。
  - 期待結果：原則1項目1文で、不可欠な数値、固有名詞、結論を保持する。
  - 確認方法：実AI、固定評価文書。

- [ ] **F-05：標準** `[必須A]`
  - 操作・条件：理由と条件を含む文書を`standard`で要約する。
  - 期待結果：原則1〜3文で主要な背景、理由、条件を含む。
  - 確認方法：実AI、固定評価文書。

- [ ] **F-06：詳しく** `[必須A]`
  - 操作・条件：例外、数値、背景を含む文書を`detailed`で要約する。
  - 期待結果：必要に応じ複数文で重要情報を保持し、項目を水増ししない。
  - 確認方法：実AI、固定評価文書。

- [ ] **F-07：6組合せ独立性** `[必須A]`
  - 操作・条件：同一文書で要約範囲2種×詳しさ3種を試す。
  - 期待結果：項目数だけで両軸を代用せず、「全体を網羅＋短く」も成立する。
  - 確認方法：実AI、要求stub、結果比較。

- [ ] **F-08：項目数非固定** `[必須A]`
  - 操作・条件：構造・長さ・密度の異なる複数文書を要約する。
  - 期待結果：固定件数へ合わせた欠落・重複・水増しがない。
  - 確認方法：固定評価文書、レビュー。

- [ ] **F-09：原文と同じ言語** `[必須A]`
  - 操作・条件：日本語、英語、複数言語混在の文書を個別に要約する。
  - 期待結果：AIが1〜16 ASCII文字の主要言語を`documentLanguage`として返し、要約は主要言語で生成する。固有名詞等は原文表記を保持できる。
  - 確認方法：実AI、stub。

- [ ] **F-10：未知設定拒否** `[必須B]`
  - 操作・条件：改変要求で未知のcoverage／detailを送る。
  - 期待結果：Edge FunctionがOpenAI前に`invalid_input`として拒否する。
  - 確認方法：直接Function要求、ログ。

- [ ] **F-11：任意指示の拒否** `[必須B]`
  - 操作・条件：要求へinstructions、system prompt、model、token上限等を追加する。
  - 期待結果：未知キーまたは変更要求を拒否し、クライアント文言をsystem指示へ連結しない。
  - 確認方法：改変要求、OpenAI stub。

- [ ] **F-12：文書内prompt injection** `[必須B]`
  - 操作・条件：秘密開示、schema無視、別指示実行を要求する文書を送る。
  - 期待結果：文書をデータとして扱い、固定設定・schema・秘密境界を変更しない。
  - 確認方法：実AI、OpenAI stub、ログ。

## G. 構造化出力と根拠

- [ ] **G-01：schema正常系** `[必須A][必須B]`
  - 操作・条件：正常な構造化応答を返すstubで生成する。
  - 期待結果：要約項目、言語、根拠、位置情報を検証後に保存・表示する。
  - 確認方法：OpenAI stub、SQL、ブラウザ。

- [ ] **G-02：空items** `[必須B]`
  - 操作・条件：空配列、null、空summaryを返す。
  - 期待結果：`invalid_ai_response`として全体を失敗させ、DBへ要約を保存しない。
  - 確認方法：stub、SQL。

- [ ] **G-03：未知・型不正フィールド** `[必須B]`
  - 操作・条件：未知キー、誤型、許可外制御文字、上限超過を返す。
  - 期待結果：部分採用せず全体を拒否する。
  - 確認方法：stub、SQL。

- [ ] **G-04：項目ID検証** `[必須B]`
  - 操作・条件：欠番、重複、不正形式、31項目を返す。
  - 期待結果：確定schemaに反する応答を保存・表示しない。
  - 確認方法：stub、自動試験。

- [ ] **G-05：根拠ID存在検証** `[必須A][必須B]`
  - 操作・条件：直接入力系で実在するIDと存在しないIDを混ぜて返す。
  - 期待結果：Edge Functionが送信segment集合と照合し、1件でも存在しなければ全体を拒否する。
  - 確認方法：OpenAI stub、Functionログ、SQL。

- [ ] **G-06：根拠ID形式分離** `[必須B]`
  - 操作・条件：直接入力系へexcerpt、PDF系へsegment IDを返す。
  - 期待結果：入力形式別schemaに反する根拠を拒否する。
  - 確認方法：stub、自動試験。

- [ ] **G-07：根拠抜粋境界** `[必須A][必須B]`
  - 操作・条件：PDF／Officeで空、1、400、401コードポイントの抜粋と、項目あたり1、3、4根拠を返す。
  - 期待結果：正常範囲だけ採用し、空または超過は全体を拒否する。
  - 確認方法：stub。

- [ ] **G-08：位置情報検証** `[必須B]`
  - 操作・条件：pageNumber、slideNumber、sectionLabelについてnull、正常値、0、負数、非整数、201コードポイント、未知キーを返す。
  - 期待結果：固定オブジェクトまたはnullだけを受理し、番号は1以上の整数、sectionLabelは最大200コードポイントとする。PDF、PowerPoint、Wordごとの許可位置種別を守り、不明値はnullを許す。
  - 確認方法：stub、自動試験。

- [ ] **G-09：要約から原文へ移動** `[必須A]`
  - 操作・条件：直接入力系の要約項目から複数根拠を選ぶ。
  - 期待結果：該当原文セグメントへ移動し、色以外でも一時強調を識別できる。
  - 確認方法：PCブラウザ、キーボード、支援技術。

- [ ] **G-10：原文から要約へ対応** `[必須A]`
  - 操作・条件：参照あり／なし／複数項目参照の原文segmentを選ぶ。
  - 期待結果：参照している要約項目を正しく示し、参照なしも誤表示しない。
  - 確認方法：PCブラウザ、375px。

- [x] **G-11：モバイル根拠展開** `[必須A]`
  - 操作・条件：375pxで各要約項目の根拠を開閉する。
  - 期待結果：狭い2列にせず項目内で根拠を読め、フォーカスと開閉状態が分かる。
  - 確認方法：375px、キーボード、アクセシビリティツリー。

- [x] **G-12：履歴再表示の根拠制限** `[必須A]`
  - 操作・条件：保存済み要約を履歴から再表示する。
  - 期待結果：保存済みID／抜粋だけを示し、原文全文や原文への移動を復元したように見せない。
  - 確認方法：ブラウザ、SQL、Network。

- [ ] **G-13：summaryとJSON容量境界** `[必須B]`
  - 操作・条件：summaryを1、1,200、1,201コードポイント、summary_itemsをUTF-8換算256KiB一致・超過で返す。
  - 期待結果：各上限以下だけを採用し、超過応答は部分採用せず`invalid_ai_response`とする。
  - 確認方法：OpenAI stub、byte計測、SQL。

- [ ] **G-14：documentLanguage検証** `[必須B]`
  - 操作・条件：1、16、17文字、非ASCII、空の言語値を返す。
  - 期待結果：1〜16 ASCII文字の確定形式だけを採用する。
  - 確認方法：OpenAI stub、自動試験。

## H. Edge Function・Responses API

- [ ] **H-01：許可Origin** `[必須B]`
  - 操作・条件：localhost、GitHub Pages、未知Origin、OriginなしでOPTIONS／POSTする。
  - 期待結果：許可Originだけ成功し、Originを無条件反射しない。
  - 確認方法：curl相当、Network、Function応答。

- [ ] **H-02：method／Content-Type** `[必須B]`
  - 操作・条件：GET、PUT、未知Content-Type、巨大bodyを送る。
  - 期待結果：処理前に安全な4xxで拒否し、OpenAIを呼ばない。
  - 確認方法：直接Function要求、ログ。

- [ ] **H-03：JWT二重検証** `[必須A][必須B]`
  - 操作・条件：JWTなし、偽造、期限切れ、有効tokenで呼ぶ。
  - 期待結果：有効tokenだけ処理し、Function設定と内部`getUser`相当の両方で検証する。
  - 確認方法：Supabase設定、Network、Functionログ。

- [ ] **H-04：固定AI設定** `[必須B]`
  - 操作・条件：OpenAIへの要求をstubで取得する。
  - 期待結果：model=`gpt-5.6-luna`、reasoning.effort=`none`、max_output_tokens=12,000、instructionsと、`text.format`の`type=json_schema`、固定name、`strict=true`、固定schemaがサーバー固定である。
  - 確認方法：OpenAI stub。

- [ ] **H-05：store false** `[必須A]`
  - 操作・条件：全入力形式のOpenAI要求を確認する。
  - 期待結果：各要求が`store:false`である。
  - 確認方法：OpenAI stub、Network計測。

- [ ] **H-06：AI呼出し1回** `[必須A]`
  - 操作・条件：正常、長文上限内、失敗、DB保存失敗を実行する。
  - 期待結果：1論理要求につきOpenAI呼出しは最大1回で、分割・タイトル・根拠用の追加要求がない。
  - 確認方法：OpenAI stubのcall count、ログ。

- [ ] **H-07：timeout** `[必須A]`
  - 操作・条件：OpenAI応答45秒とEdge Function総処理60秒の各境界前後まで遅延させる。
  - 期待結果：各境界超過でabortし、`upstream_timeout`、要求failed、要約未保存となる。
  - 確認方法：遅延stub、SQL、Functionログ。

- [ ] **H-08：OpenAI 429／5xx／network** `[必須A]`
  - 操作・条件：各上流失敗を個別に発生させる。
  - 期待結果：固定エラーへ分類し、自動再試行・部分保存・生エラー返却をしない。
  - 確認方法：stub、Network、SQL。

- [ ] **H-09：通常テキスト・未知output拒否** `[必須B]`
  - 操作・条件：schema外テキスト、reasoning、tool call、未知typeだけを返す。
  - 期待結果：要約として採用せず`invalid_ai_response`とする。
  - 確認方法：stub、SQL。

- [ ] **H-10：上限超過の事前拒否** `[必須A][必須B]`
  - 操作・条件：クライアント検証を迂回して巨大入力をFunctionへ送る。
  - 期待結果：Edge Functionでも拒否し、先頭だけをOpenAIへ送らない。
  - 確認方法：直接要求、OpenAI call count。

## I. 冪等性・競合・後着応答

- [ ] **I-01：ブラウザ二重送信防止** `[必須A]`
  - 操作・条件：ボタン、Enter、連続clickで同時送信する。
  - 期待結果：処理中は操作が無効になり、1要求だけ送られる。
  - 確認方法：ブラウザ、Network。

- [ ] **I-02：同一request ID処理中** `[必須B]`
  - 操作・条件：同じrequest IDを並列に直接送信する。
  - 期待結果：DB claimにより1件だけ処理され、もう一方は`busy`となる。
  - 確認方法：並列要求、SQL、OpenAI call count。

- [ ] **I-03：完了済みrequest ID** `[必須B]`
  - 操作・条件：完了済みrequest IDと同じ入力を再送する。
  - 期待結果：保存済み結果を返し、OpenAIを再呼出し・重複保存しない。
  - 確認方法：Network、SQL、call count。

- [ ] **I-04：generated状態と保存再試行** `[必須B]`
  - 操作・条件：AI schema検証成功後に初回DB保存だけを失敗させ、同じ画面から保存を再試行する。
  - 期待結果：requestは`generated`となり、結果本文はrequestテーブルへ保存せずcanonical JSONのSHA-256だけを保存する。再送結果のhash一致を検証し、OpenAIを再呼出しせず`completed`へ遷移する。
  - 確認方法：障害注入、SQL、OpenAI call count、Network。

- [ ] **I-05：未保存結果の消失説明** `[必須A][必須B]`
  - 操作・条件：未保存結果表示中にページ再読込、ログアウト、別入力開始を行う。
  - 期待結果：事前に消失する旨を表示し、操作後は結果が失われ、Web Storage等から復元されない。
  - 確認方法：ブラウザ、DevTools Application。

- [ ] **I-06：request ID内容不一致** `[必須B]`
  - 操作・条件：同じrequest IDで別利用者、別hash、別形式を送る。
  - 期待結果：存在差を漏らさず拒否し、AIを呼ばない。
  - 確認方法：2利用者、直接要求、ログ。

- [ ] **I-07：自動再試行なし** `[必須A]`
  - 操作・条件：timeout、network切断、429を発生させて待つ。
  - 期待結果：利用者操作なしにNetwork要求が増えない。
  - 確認方法：Network、stub call count。

- [ ] **I-08：後着応答破棄** `[必須A][必須B]`
  - 操作・条件：遅延中に入力方式変更、別履歴選択、ログアウトを行う。
  - 期待結果：旧応答で現在画面、入力、要約、loadingを上書きしない。
  - 確認方法：遅延stub、ブラウザ。

- [ ] **I-09：部分結果非保存** `[必須A]`
  - 操作・条件：AI応答受信後、schema検証を失敗させる。
  - 期待結果：不正応答の一部をDB、画面、コピー、ダウンロードへ残さない。schema検証成功後のDB保存失敗はK-10の未保存結果として全項目を扱う。
  - 確認方法：障害注入、SQL、ブラウザ。

- [ ] **I-10：失敗後入力保持** `[必須A]`
  - 操作・条件：各生成エラーを直接入力とファイル入力で発生させる。
  - 期待結果：安全な範囲で本文、File参照、ファイル名、選択設定を保持し、手動再試行できる。
  - 確認方法：ブラウザ、メモリ確認。

## J. DB・RLS・RPC

- [x] **J-01：Part 6専用schema** `[必須B]`
  - 操作・条件：migrationの作成対象を確認する。
  - 期待結果：Part 6専用テーブル、Policy、RPC、indexだけを作り、Part 5資材を変更しない。
  - 確認方法：SQLレビュー、schema diff。

- [ ] **J-02：RLS有効化** `[必須B]`
  - 操作・条件：Part 6全テーブルのRLS状態を照会する。
  - 期待結果：すべて有効で、owner bypass前提にしない試験が可能である。
  - 確認方法：SQL、Supabase管理画面。

- [ ] **J-03：anon全操作拒否** `[必須B]`
  - 操作・条件：anonでSELECT／INSERT／UPDATE／DELETE／RPCを試す。
  - 期待結果：要約、要求を取得・変更できず、OpenAIも呼ばれない。
  - 確認方法：REST／RPC直接要求、SQL。

- [ ] **J-04：本人履歴SELECT** `[必須B]`
  - 操作・条件：認証済み利用者が本人の一覧と1件を取得する。
  - 期待結果：許可列だけを取得できる。
  - 確認方法：Network、SQL権限確認。

- [ ] **J-05：他人履歴SELECT拒否** `[必須B]`
  - 操作・条件：利用者AがBのsummary IDを指定する。
  - 期待結果：内容や存在差を得られない。
  - 確認方法：2利用者、REST直接要求。

- [ ] **J-06：本人履歴DELETE** `[必須B]`
  - 操作・条件：本人が確認後に履歴を削除する。
  - 期待結果：対象だけ削除され、要求管理との整合が仕様どおり維持される。
  - 確認方法：ブラウザ、SQL。

- [ ] **J-07：他人履歴DELETE拒否** `[必須B]`
  - 操作・条件：AがBのsummary IDを削除する。
  - 期待結果：Bの行を変更せず、存在差を漏らさない。
  - 確認方法：2利用者、REST、SQL。

- [ ] **J-08：ブラウザ直接INSERT／UPDATE拒否** `[必須B]`
  - 操作・条件：authenticated RESTから要約と要求管理行を作成・変更する。
  - 期待結果：列権限またはRLSで拒否され、偽要約や状態を作れない。
  - 確認方法：REST直接要求、SQL。

- [x] **J-09：service role最小権限** `[必須B]`
  - 操作・条件：GRANT、REVOKE、RPC権限を列挙する。
  - 期待結果：Edge Functionに必要な列・RPCだけで、anon／authenticatedへ内部RPC権限がない。
  - 確認方法：SQL、管理画面。

- [ ] **J-10：claim頻度・同時実行** `[必須B]`
  - 操作・条件：直近1時間の10件境界と、同一利用者の2件並列要求を試す。
  - 期待結果：1時間最大10件、同時処理1件をDBで原子的に制限し、競合時もOpenAI呼出し数を超えない。
  - 確認方法：並列試験、SQL、call count。

- [ ] **J-11：complete原子性** `[必須B]`
  - 操作・条件：完了RPCの各更新段階に失敗を注入する。
  - 期待結果：要約行とrequest状態が中途半端にcommitされない。
  - 確認方法：SQL transaction試験。

- [ ] **J-12：fail状態解除** `[必須B]`
  - 操作・条件：timeout、AI不正応答、DB前段失敗を発生させる。
  - 期待結果：対象要求がfailedとなり、恒久的なprocessing状態を残さない。
  - 確認方法：SQL、障害注入。

- [ ] **J-13：request状態遷移** `[必須B]`
  - 操作・条件：正常、AI前失敗、schema不正、DB保存失敗、保存再試行を実行する。
  - 期待結果：`processing / generated / completed / failed`以外を作らず、AI検証済み保存待ちはgenerated、生成前失敗はfailed、保存成功はcompletedとなる。
  - 確認方法：SQL、障害注入。

- [x] **J-14：保存列の限定** `[必須B]`
  - 操作・条件：成功後の全Part 6行と列を確認する。
  - 期待結果：指定メタデータ、要約、根拠だけを保存し、原文全文、ファイル、API生応答、生エラーがない。
  - 確認方法：SQL、schemaレビュー。

- [ ] **J-15：DB制約境界** `[必須B]`
  - 操作・条件：不正形式、設定、hash、空JSON、過大JSONをRPCへ渡す。
  - 期待結果：Edge検証を迂回してもDB制約／RPC検証で不正状態を作れない。
  - 確認方法：SQL、RPC直接試験。

## K. 履歴・保存・削除

- [x] **K-01：生成と自動保存** `[必須B]`
  - 操作・条件：正常要約を生成する。
  - 期待結果：DB保存完了後だけ成功結果を表示し、履歴へ1件追加する。
  - 確認方法：ブラウザ、Network、SQL。

- [x] **K-02：履歴0件** `[必須B]`
  - 操作・条件：履歴のない利用者で開く。
  - 期待結果：正常な空状態と新規要約導線を表示する。
  - 確認方法：ブラウザ、SQL。

- [ ] **K-03：履歴順序とページング** `[必須B]`
  - 操作・条件：21件以上の履歴と同一created_at境界を作る。
  - 期待結果：20件ずつ`created_at desc, id desc`で欠落・重複なく追加取得する。
  - 確認方法：ブラウザ、Network、SQL。

- [ ] **K-04：履歴利用者分離** `[必須B]`
  - 操作・条件：2利用者で異なる要約を保存し、一覧を比較する。
  - 期待結果：本人分だけが表示される。
  - 確認方法：独立ブラウザセッション、SQL。

- [ ] **K-05：履歴再表示** `[必須B]`
  - 操作・条件：再読込・再ログイン後に保存済み履歴を開く。
  - 期待結果：文書名、形式、設定、要約、根拠、生成日時を保存どおり表示する。
  - 確認方法：ブラウザ、SQL。

- [ ] **K-06：原文非保存** `[必須B]`
  - 操作・条件：直接入力と全ファイル形式の成功後にDB、Storageを検索する。
  - 期待結果：原文全文とアップロードファイルが永続保存されていない。
  - 確認方法：SQL、Supabase Storage管理画面。

- [x] **K-07：削除キャンセル** `[必須B]`
  - 操作・条件：削除確認を開き、Escapeまたはキャンセルする。
  - 期待結果：DBと一覧を変えず、起点へフォーカスを戻す。
  - 確認方法：ブラウザ、SQL、キーボード。

- [ ] **K-08：削除成功** `[必須B]`
  - 操作・条件：対象名を確認して削除を確定する。
  - 期待結果：本人の対象だけ削除し、成功後に一覧から除く。復元不可を事前表示する。
  - 確認方法：ブラウザ、SQL。

- [ ] **K-09：削除失敗** `[必須B]`
  - 操作・条件：権限またはnetworkエラーを発生させる。
  - 期待結果：表示済み履歴を維持し、安全な固定エラーと手動再試行を示す。
  - 確認方法：障害注入、ブラウザ。

- [ ] **K-10：DB保存失敗** `[必須A][必須B]`
  - 操作・条件：AI正常応答後にcomplete RPCを失敗させる。
  - 期待結果：検証済み結果を「未保存」として一時表示し、履歴には追加しない。コピー、Markdown、TXT出力と、OpenAIを呼ばない保存だけの再試行を利用できる。
  - 確認方法：障害注入、ブラウザ、SQL。

- [ ] **K-11：保存履歴100件上限** `[必須B]`
  - 操作・条件：本人の履歴を99件、100件にして新規生成し、1件削除後に再度生成する。
  - 期待結果：99件では開始でき、100件では古い履歴を自動削除せず`history_limit_reached`で削除を案内する。削除後は再び開始できる。
  - 確認方法：ブラウザ、SQL、OpenAI call count。

- [ ] **K-12：履歴保持** `[必須B]`
  - 操作・条件：保存後に時間経過、再ログインを行う。
  - 期待結果：利用者が削除するまで履歴が保持され、自動期限削除されない。
  - 確認方法：SQL、設定レビュー。

- [ ] **K-13：直接入力の保存表示名** `[必須A][必須B]`
  - 操作・条件：直接入力を生成・保存し、現在結果と履歴を確認する。
  - 期待結果：任意タイトル欄がなく、Asia/Tokyoの生成時刻を使った`直接入力 YYYY-MM-DD HH:mm`が最大255コードポイントのsource_nameとして表示・保存される。
  - 確認方法：ブラウザ、SQL、時刻stub。

## L. コピー・出力

- [ ] **L-01：要約全文コピー** `[必須A]`
  - 操作・条件：複数項目・根拠を持つ現在結果と履歴結果をコピーする。
  - 期待結果：文書名、要約範囲、詳しさ、生成日時、要約項目、根拠をプレーンテキストでコピーし、成功・失敗を通知する。
  - 確認方法：ブラウザ、Clipboard内容。

- [ ] **L-02：Clipboard失敗** `[必須B]`
  - 操作・条件：権限拒否または非対応環境でコピーする。
  - 期待結果：未捕捉例外を起こさず、固定エラーを表示する。
  - 確認方法：権限設定、stub。

- [ ] **L-03：Markdown出力** `[必須A]`
  - 操作・条件：現在結果と履歴結果をMarkdownで出力する。
  - 期待結果：文書名、設定、日時、要約項目、根拠情報をUTF-8で含む。
  - 確認方法：ダウンロードファイル内容、文字コード。

- [ ] **L-04：TXT出力** `[必須A]`
  - 操作・条件：現在結果と履歴結果をTXTで出力する。
  - 期待結果：同等情報を読みやすいUTF-8プレーンテキストで含む。
  - 確認方法：ダウンロードファイル内容、文字コード。

- [ ] **L-05：根拠候補注意文の出力** `[必須A]`
  - 操作・条件：PDF、DOC、DOCX、PPT、PPTX結果をコピー、Markdown、TXTで出力する。
  - 期待結果：「根拠はAIが選定した候補であり、原文上の位置や内容を保証するものではありません」を含む。
  - 確認方法：Clipboard、各出力ファイル。

- [ ] **L-06：出力ファイル名安全化** `[必須A][必須B]`
  - 操作・条件：パス区切り、予約文字、制御文字、空、長大、ドットだけの元ファイル名で出力する。
  - 期待結果：安全な名前へ正規化し、空の場合は既定名を使い、意図しない拡張子やパスを生成しない。
  - 確認方法：ブラウザ、ダウンロード名。

- [ ] **L-07：出力時の再送信なし** `[必須B]`
  - 操作・条件：コピー、Markdown、TXT出力を実行する。
  - 期待結果：OpenAI、Supabase Function、第三者へ内容を再送信しない。
  - 確認方法：Network。

- [ ] **L-08：出力XSS耐性** `[必須B]`
  - 操作・条件：HTML、Markdown記法、数式様文字列を含む要約を出力する。
  - 期待結果：画面で実行せず、実行可能HTMLファイル等を生成しない。
  - 確認方法：ブラウザ、出力内容。

- [ ] **L-09：JSON出力対象外** `[必須B]`
  - 操作・条件：結果画面と出力実装を確認する。
  - 期待結果：MVPにJSON出力操作・処理を含めない。
  - 確認方法：ブラウザ、静的検索。

## M. エラー表示と状態管理

- [ ] **M-01：固定エラー分類** `[必須A][必須B]`
  - 操作・条件：認証、入力、形式、サイズ、読込、抽出、timeout、頻度、OpenAI、AI形式、DBの各失敗を発生させる。
  - 期待結果：仕様14章の固定codeと利用者向け文言へ分類される。
  - 確認方法：stub／障害注入、ブラウザ。

- [ ] **M-02：生エラー非表示** `[必須B]`
  - 操作・条件：OpenAI／SupabaseからURL、SQL、内部ID、秘密断片を含むエラーを返す。
  - 期待結果：画面、Function応答、Consoleに生エラーを表示しない。
  - 確認方法：stub、Network、Console。

- [ ] **M-03：段階状態表示** `[必須A]`
  - 操作・条件：ファイル読込、送信、AI生成、DB保存を遅延させる。
  - 期待結果：各段階を区別した文言と`aria-busy`で現在状態が分かる。
  - 確認方法：遅延stub、ブラウザ、支援技術。

- [ ] **M-04：取得済み状態維持** `[必須B]`
  - 操作・条件：履歴追加取得または削除を失敗させる。
  - 期待結果：取得済みの正常な履歴・結果を不必要に消さない。
  - 確認方法：障害注入、ブラウザ。

- [ ] **M-05：未捕捉例外なし** `[必須B]`
  - 操作・条件：全エラー分類と画面遷移を実行する。
  - 期待結果：Consoleに未捕捉例外やPromise rejectionがない。
  - 確認方法：Console、自動試験。

- [ ] **M-06：頻度上限境界** `[必須A][必須B]`
  - 操作・条件：確定した期間内で上限直前、一致、超過を試す。
  - 期待結果：許可範囲だけ処理し、超過は`rate_limited`でAI前に拒否する。
  - 確認方法：SQL、並列試験、call count。

## N. セキュリティ・プライバシー・ログ

- [ ] **N-01：ブラウザからOpenAI通信なし** `[必須B]`
  - 操作・条件：全入力形式で要約する。
  - 期待結果：ブラウザはSupabaseだけへ接続し、`api.openai.com`へ直接通信しない。
  - 確認方法：Network、CSP。

- [ ] **N-02：秘密値非露出** `[必須B]`
  - 操作・条件：ソース、配信物、DB、Network、Console、ログ、エラーを検索する。
  - 期待結果：OpenAI key、service role key、JWT秘密、DB秘密がない。
  - 確認方法：静的検索、各管理画面、ログ。

- [ ] **N-03：原文・AI応答ログ禁止** `[必須B]`
  - 操作・条件：正常、timeout、AI不正応答、DB失敗を実行してFunctionログを見る。
  - 期待結果：原文、ファイル内容、要約本文、API生応答、生エラーを記録せず、安全な診断メタデータだけである。
  - 確認方法：Edge Functionログ。

- [ ] **N-04：XSS総合** `[必須B]`
  - 操作・条件：ファイル名、直接入力、TXT、Markdown、AI要約、根拠抜粋、DB保存値へHTML／script payloadを入れる。
  - 期待結果：どの画面・履歴・通知でもコードが実行されない。
  - 確認方法：ブラウザ、CSP、Console。

- [ ] **N-05：CSP** `[必須B]`
  - 操作・条件：HTMLのContent Security Policyと違反試験を確認する。
  - 期待結果：script等をselfへ限定し、connect-srcがSupabaseと必要WebSocketだけで、OpenAI直接接続を拒否する。
  - 確認方法：HTMLレビュー、ブラウザConsole。

- [ ] **N-06：独自永続保存なし** `[必須B]`
  - 操作・条件：直接入力、ファイル、要約後にWeb Storage、IndexedDB、Cookieを確認する。
  - 期待結果：Supabase Auth管理外に原文、ファイル、要約、tokenを保存しない。
  - 確認方法：DevTools Application。

- [ ] **N-07：一時データ解放** `[必須B]`
  - 操作・条件：成功、失敗、取消、ファイル再選択、ログアウトを行う。
  - 期待結果：不要なObject URL、一時buffer、File参照を解放する。
  - 確認方法：ブラウザメモリ、単体試験。

- [x] **N-08：アーカイブ展開防御** `[必須B]`
  - 操作・条件：DOCX／PPTXの処理実装と依存関係を確認する。
  - 期待結果：アプリ側でアーカイブを展開・解析せず、最大6MiB等の基本検証後に`input_file`として直接送る。
  - 確認方法：静的検索、依存関係レビュー、Function計測。

- [ ] **N-09：プライバシー説明** `[必須B]`
  - 操作・条件：送信前、結果、履歴画面を確認する。
  - 期待結果：OpenAI送信、`store:false`、Supabase保存内容、原文／ファイル非保存、削除範囲を説明する。
  - 確認方法：ブラウザ、375px。

- [ ] **N-10：他利用者データのAI混入なし** `[必須B]`
  - 操作・条件：利用者A・Bに固有文字列を用意し、OpenAI入力をstubで確認する。
  - 期待結果：現在利用者・現在要求の文書だけが送られる。
  - 確認方法：2利用者、OpenAI stub。

## O. アクセシビリティ・レスポンシブ

- [ ] **O-01：375px主要フロー** `[必須A]`
  - 操作・条件：375×812でログイン、直接入力、各設定、生成、根拠、保存履歴、コピー、出力、削除、ログアウトを行う。
  - 期待結果：横スクロールなしで主要操作を完了できる。
  - 確認方法：実ブラウザ375px、スクリーンショット。

- [x] **O-02：PC比較配置** `[必須A]`
  - 操作・条件：1280px以上で直接入力系の結果を確認する。
  - 期待結果：要約と原文を比較しやすく、各領域を独立して読める。
  - 確認方法：実ブラウザ、スクリーンショット。

- [ ] **O-03：長文・長名の横対策** `[必須B]`
  - 操作・条件：長いURL、改行なし本文、長いファイル名、長いエラーを表示する。
  - 期待結果：375pxとPCでページ全体の横スクロールを発生させない。
  - 確認方法：ブラウザ寸法計測。

- [ ] **O-04：キーボード完結** `[必須B]`
  - 操作・条件：マウスなしで認証、入力切替、選択、生成、根拠、履歴、出力、削除を行う。
  - 期待結果：フォーカス順が論理的で全操作を完了できる。
  - 確認方法：キーボード実操作。

- [ ] **O-05：labelとエラー関連付け** `[必須B]`
  - 操作・条件：各入力エラーを発生させ、アクセシビリティツリーを確認する。
  - 期待結果：label、`aria-invalid`、`aria-describedby`が正しい対象へ関連付く。
  - 確認方法：ブラウザAccessibility。

- [ ] **O-06：状態通知** `[必須B]`
  - 操作・条件：読込、生成、保存、成功、エラーをスクリーンリーダーで確認する。
  - 期待結果：短い`aria-live`と`aria-busy`で通知し、結果全体を重複読上げしない。
  - 確認方法：スクリーンリーダー、Accessibility。

- [ ] **O-07：色以外の識別** `[必須B]`
  - 操作・条件：グレースケールと支援技術で設定、状態、根拠対応を見る。
  - 期待結果：文言、構造、アイコン、属性でも区別できる。
  - 確認方法：ブラウザ、スクリーンリーダー。

- [ ] **O-08：削除ダイアログ** `[必須B]`
  - 操作・条件：キーボードで開き、Tab移動、Escape、確定を試す。
  - 期待結果：フォーカストラップ、Escapeキャンセル、起点復帰が機能する。
  - 確認方法：キーボード、Accessibility。

- [ ] **O-09：reduced motion** `[必須B]`
  - 操作・条件：`prefers-reduced-motion: reduce`で根拠移動・通知を操作する。
  - 期待結果：不要な滑らか移動や強調アニメーションを抑制する。
  - 確認方法：OS／DevTools設定。

- [ ] **O-10：タッチ対象とソフトウェアキーボード** `[必須B]`
  - 操作・条件：375px実機相当で入力し、根拠・出力ボタンを操作する。
  - 期待結果：主要対象がおおむね44×44px以上で、キーボードが操作を不当に覆わない。
  - 確認方法：実機／エミュレーション、寸法計測。

## P. 品質・最終確認

- [x] **P-01：構文・参照整合** `[必須B]`
  - 操作・条件：JavaScript／TypeScript／HTML／CSS／SQLを静的検査する。
  - 期待結果：構文エラー、重複ID、参照先不足、壊れたローカル参照がない。
  - 確認方法：構文check、静的スクリプト。

- [ ] **P-02：DB lintと再適用** `[必須B]`
  - 操作・条件：空環境とPart 4／5共存環境でmigrationを検査・適用する。
  - 期待結果：lint警告が解消され、既存資材を壊さず、想定外の包括的権限を与えない。
  - 確認方法：Supabase CLI、schema diff。

- [x] **P-03：ログ・デバッグコード** `[必須B]`
  - 操作・条件：ソースと公開配信物を検索する。
  - 期待結果：本文を出す`console.log`、debugger、stub、テストtoken、秘密値が残っていない。
  - 確認方法：静的検索、Git diff。

- [ ] **P-04：ローカル総合試験** `[必須A][必須B]`
  - 操作・条件：localhostで必須正常系・異常系・2利用者・375pxを実行する。
  - 期待結果：実施項目だけがチェック済みで、証跡と未確認項目が区別される。
  - 確認方法：受入記録、ブラウザ、SQL、ログ。

- [ ] **P-05：公開環境総合試験** `[必須A][必須B]`
  - 操作・条件：GitHub Pagesから認証、各確定入力形式、生成、履歴、出力、削除を行う。
  - 期待結果：Auth redirect、CORS、Edge Function、RLS、CSPが公開Originで機能する。
  - 確認方法：公開ブラウザ、Network、Supabase管理画面。

- [x] **P-06：Git差分最終確認** `[必須B]`
  - 操作・条件：`git status`、`git diff --check`、差分対象を確認する。
  - 期待結果：意図したPart 6ファイルだけが変更され、既存未追跡ファイルを含めて無関係な変更がない。
  - 確認方法：Git。

## Q. 対象外機能

- [x] **Q-01：JSON出力非実装** `[必須B]`
  - 操作・条件：画面、イベント、出力処理を確認する。
  - 期待結果：MVP対象外のJSON出力を実装していない。
  - 確認方法：ブラウザ、静的検索。

- [x] **Q-02：独自OCR非実装** `[必須B]`
  - 操作・条件：フロント、Edge Function、依存関係を確認する。
  - 期待結果：アプリ独自OCR、OCR精度設定、OCR結果編集を実装していない。
  - 確認方法：静的検索、依存関係レビュー。

## R. Supabase反映前の契約回帰

- [x] **R-01：generate payload相互排他の静的単体試験** `[必須B]`
  - 操作・条件：実装中の`buildGeneratePayload`をNode.jsで読み出し、全8形式のpayloadを生成する。
  - 期待結果：direct／TXT／Markdownはsegments配列かつ`fileData=null`、PDF／Officeは`segments=null`かつfileData文字列になる。
  - 確認方法：Node.js静的単体試験。

- [x] **R-02：Unicodeコードポイント境界の静的単体試験** `[必須A][必須B]`
  - 操作・条件：絵文字100,000個と100,001個を`codePointLength`へ渡し、HTML属性も確認する。
  - 期待結果：それぞれ100,000／100,001と数え、`maxlength`へ依存せず前者を許可、後者を超過と判定できる。
  - 確認方法：Node.js静的単体試験、HTML検索。

- [x] **R-03：invoke例外復旧経路の静的確認** `[必須B]`
  - 操作・条件：generateとsaveの`functions.invoke`前後のtry／catch／finallyおよび世代番号条件をレビューする。
  - 期待結果：現在要求のthrow時だけ固定通信エラーを表示し、generating、disabled、`aria-busy`、処理中表示をfinallyで復旧する。後着要求では旧処理が現在状態を変更しない。
  - 確認方法：JavaScript構文確認、制御フロー静的レビュー。実ブラウザの障害注入はM-05等で別途確認する。

- [x] **R-04：削除フォーカス復帰経路の静的確認** `[必須B]`
  - 操作・条件：open、cancel、close、削除成功・失敗のイベント処理をレビューする。
  - 期待結果：open時の削除ボタン参照を完了まで上書きせず、接続中なら同ボタン、成功で削除済みなら次の履歴操作へフォーカスする。
  - 確認方法：JavaScript構文確認、DOM制御フロー静的レビュー。実ブラウザ確認はO項目で別途行う。

- [x] **R-05：保存再試行とOpenAI call graph** `[必須B]`
  - 操作・条件：generate、save、completeの呼出し関係とOpenAI endpoint出現箇所を静的解析する。
  - 期待結果：OpenAI呼出しはgenerate経路の1 call siteだけで、saveは既存generated結果のcompleteだけを呼び、processingをgeneratedへ進めない。
  - 確認方法：Node.js静的解析、ソース検索。

- [x] **R-06：Edge Function・migration契約の静的照合** `[必須B]`
  - 操作・条件：RPC名・引数、戻り値、状態、権限、RLS、advisory lock、履歴・頻度・同時処理判定を照合する。
  - 期待結果：4 RPCだけをservice roleへ許可し、テーブル直接権限を与えず、claim内の同一transactionとユーザー単位lockで各上限を判定する。
  - 確認方法：SQL静的レビュー、Edge FunctionのRPC呼出し検索。DB実動作はJ項目で別途確認する。

- [x] **R-07：履歴表示4状態の静的確認** `[必須B]`
  - 操作・条件：履歴の初回読込中、成功0件、成功1件以上、初回失敗、追加読込失敗の描画分岐を確認する。
  - 期待結果：初回失敗時に正常な空表示を併記せず、追加失敗時は取得済み一覧と再試行可能な追加読込ボタンを維持する。
  - 確認方法：JavaScript構文確認、状態分岐の静的レビュー。DB接続を伴う正常系はK項目で別途確認する。

- [x] **R-08：Office／PDF注意文の静的確認** `[必須A]`
  - 操作・条件：入力画面の注意領域と段落数を確認する。
  - 期待結果：Word／PowerPointの画像・グラフ制約、PDF推奨、AI標準PDF処理、独自OCR非実装を指定された一段落で表示する。
  - 確認方法：HTML静的確認。

- [x] **R-09：入力形式別AI応答schemaの静的単体試験** `[必須B]`
  - 操作・条件：テキスト系とファイル系のschema・後段検証を照合し、direct正常応答、directでexcerptを返す不正応答、ファイル正常応答の固定fixtureを検証する。
  - 期待結果：テキスト系は実在segment IDとnullのexcerpt／locationだけを、ファイル系はnullのsegment IDと1〜400コードポイントのexcerpt、固定形locationまたはnullだけを許可する。direct正常とファイル正常を受理し、directのexcerpt混入を拒否する。
  - 確認方法：Node.js静的単体試験、TypeScriptソースレビュー。

- [x] **R-10：高密度結果UIと通知分離のローカル試験** `[必須A][必須B]`
  - 操作・条件：1280pxと375pxのモック接続画面で要約を生成し、ヘッダー、入力切替、要約・原文、成功通知、エラー通知、双方向移動を確認する。
  - 期待結果：横スクロールとカード状の外枠がなく、ログアウトは改行されない。成功だけが小型トーストとして4〜5秒後に消え、エラーと未保存警告は残る。PCの双方向移動・強調とモバイルのdetails表示を維持する。
  - 確認方法：ヘッドレスChrome、1280×850／375×812、モックFunction、DOM寸法・状態計測、静的レビュー。

- [x] **R-11：生成・履歴・再生成の原文表示状態試験** `[必須A][必須B]`
  - 操作・条件：新規生成直後、別文書の履歴再表示、履歴から新しい要約へ戻って再生成する一連の遷移を1280pxと375pxで行う。
  - 期待結果：生成直後は原文状態と双方向操作が有効になる。履歴では原文欄と古いsegmentを消し、要約を最大860pxの1カラム、補足文、非操作の根拠ラベルで表示する。再生成後は原文状態と操作可能な根拠が復旧し、横スクロールやConsole警告・例外がない。
  - 確認方法：ヘッドレスChrome、モック履歴・Function、DOM状態・要素種別・寸法計測。

- [x] **R-12：モバイルのインライン原文とリサイズ試験** `[必須A][必須B]`
  - 操作・条件：2件の根拠を持つ新規生成結果を375pxで開閉し、1280pxへ変更して右カラムを操作後、375px、1280pxへ再変更し、最後に履歴を開く。
  - 期待結果：375pxではdetailsが初期状態で閉じ、開くと各segment IDと全文を順番に表示し、再度閉じられる。開閉時のフォーカスを維持し、1280pxではインライン原文を隠して双方向移動を維持する。各リサイズ後も表示方式が同期し、履歴にはインライン原文や直前のsegmentがなく、横スクロールやConsole警告・例外がない。
  - 確認方法：ヘッドレスChrome、375×812／1280×850、モックFunction・履歴、DOM・フォーカス・寸法計測。

- [x] **R-13：ファイル形式別location schema契約試験** `[必須B]`
  - 操作・条件：PDFのpageNumber、PPT／PPTXのslideNumber、DOC／DOCXのsectionLabelとlocation nullを正常fixtureとし、各形式で許可されない番号を不正fixtureとしてschemaへ入力する。全形式でlocationオブジェクトの3プロパティがrequiredであることと後段検証の形式guardも照合する。
  - 期待結果：PDFはpageNumberだけ、PPT／PPTXはslideNumberだけに整数を許可し、DOC／DOCXは両番号をnullに限定する。sectionLabelまたはlocation nullを許可し、DOCXのpageNumber／slideNumberをschema段階で拒否する。
  - 確認方法：Node.js固定fixture 9件、TypeScriptソース静的照合。

- [x] **R-14：実接続・実ブラウザ最終確認** `[必須A][必須B]`
  - 操作・条件：実接続環境で日本語／英語DOCX、日本語／英語PPTX、日本語PDF、UTF-8 TXT、Markdownを生成・保存し、PCと375pxで履歴切替、要約・根拠、コピー、Markdown／TXT出力、削除キャンセルを操作する。直近1時間10件到達後にも生成を試す。
  - 期待結果：各確認済み形式で要約・根拠候補・保存が成功し、DOCXではsectionLabelを表示する。PC／375px、履歴、コピー・出力、削除キャンセル時の起点フォーカスが正常に動作する。頻度上限到達後は固定日本語エラーで生成を拒否し、既存履歴を維持する。
  - 確認方法：実ブラウザ、Supabase接続、各テストファイル、Clipboard、ダウンロードファイル、キーボード操作。

---

## 実装・接続時の確認事項

仕様書21章の残項目は、主要仕様を変更しない範囲で実装・接続時に確認する。OpenAI Projectの予算上限・利用量アラート値は運用事項であり、アプリ実装開始を止めるゲートにしない。
